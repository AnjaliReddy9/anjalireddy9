# Anjali Ega

Software engineer, five years. Lately working on retrieval, agents, and orchestration for LLM applications, plus the eval and observability tooling around them.

## What I'm building

**Lumen.** Natural language analytics that runs against real warehouses, not toy schemas. Semantic layer in YAML, schema-aware query validation, ambiguity resolution before execution. Benchmarked on Spider, BIRD, and a 60-table public dataset.
Python, Postgres, DuckDB, Claude, Angular.

**Atlas.** Enterprise search across Slack, Notion, Drive, and GitHub with permission-aware retrieval — users only see results they could see in the source system. Hybrid retrieval: BM25, dense vectors, cross-encoder reranking. Span-level citations.
Python, Tantivy, Qdrant, Angular.

**Sentinel.** GitHub app for code review. Repo-aware context so it understands callers and related tests, not just the diff. Learns from accepted vs dismissed comments. Currently installed on [N] open-source projects.
Python, tree-sitter, Postgres.

**Conductor.** Workflow engine for AI agents. Durable execution, deterministic replay, survives process restarts and pauses for human approval that may take days. Visual debugger shows token usage and cost per node.
Java, Postgres, ClickHouse, Angular.

**Echo.** Real-time voice assistant for sales calls. Sub-300ms transcription, live CRM lookups during the conversation, structured post-call summaries that sync back to Salesforce.
WebRTC, Deepgram, GPT-4o realtime, Angular.

## Writing

- Permission-aware RAG: the trust model most demos skip
- Why your agent platform won't survive a process restart
- NL→SQL on real schemas: notes from a 60-table benchmark
- Hybrid retrieval, honestly benchmarked

## Stack

Python, Java. Angular. Postgres, ClickHouse, Redis. Kubernetes, Terraform. Claude, GPT, Llama. vLLM, Whisper, Pyannote.

## Contact

[LinkedIn] · [X] · [Blog] · anjali@example.com
